import "./TimerActions.css";
import { TimerActionsProps } from "./TimerActions.types";

const TimerActions = (props: TimerActionsProps) => {
  return <div className="timer-actions">timer actions here</div>;
};

export default TimerActions;
